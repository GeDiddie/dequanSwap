# Solana RPC used by the browser for balance reads + submitting signed transactions
VITE_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# dequanW Trading API WebSocket (public edge URL in prod, localhost in dev)
VITE_DEQUANW_WS_URL=ws://localhost:8900

# Auth (MVP):
# - For local/dev you can use an API key (not safe for public websites)
# - For public hosting, use short-lived tokens minted by an auth service and pass them as VITE_DEQUANW_AUTH_TOKEN
VITE_DEQUANW_API_KEY=
VITE_DEQUANW_AUTH_TOKEN=
