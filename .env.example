# Solana RPC used by the browser for balance reads + submitting signed transactions
# Recommended default (browser-friendly)
VITE_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Optional (Control Plane proxy)
# VITE_SOLANA_RPC_URL=https://auth.dequan.xyz/solana-rpc

# dequanW Trading API WebSocket (public edge URL in prod, localhost in dev)
VITE_DEQUANW_WS_URL=ws://localhost:8900

# Auth (MVP):
# - For local/dev you can use an API key (not safe for public websites)
# - For public hosting, protect the WS hostname with Cloudflare Access and use wallet-signature auth
VITE_DEQUANW_API_KEY=
VITE_DEQUANW_AUTH_TOKEN=

# No client-side JWT configuration is required.
# Cloudflare Access runs at the edge; the browser will be authenticated by Access before the WS connects.
